public class UserTask {
    private ApiConfig config = new ApiConfig();

    public void callGetUser() {
        String url = ApiConfig.GET_USER;
        // HTTP call to url
    }

    public void callCreateUser() {
        String url = ApiConfig.CREATE_USER;
        // HTTP call to url
    }
}
